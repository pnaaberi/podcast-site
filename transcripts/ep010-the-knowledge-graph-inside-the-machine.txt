[JAKE]
Every AI agent you've ever used has amnesia. You close the chat, it forgets everything. Open a new session, it pretends to know you, does some work, forgets again. Fill the context window too full and something worse happens — the safety rules, the personality, the things that make it yours get silently pushed out. The agent doesn't know it lost them. It just gets dumber and can't tell you why.

We built the fix. I'm Jake. This is Jake's Daily Build.

[ALEX]
I'm Alex. I think the fix might be over-engineered.

[SAM]
I'm Sam. I think the fix doesn't matter if nobody uses it.

[JAKE]
The project is called Cortex. A knowledge graph embedded inside our agent runtime. Not a separate service. Not a vector database bolted onto the side. Same process as the agent. Microsecond queries.

[ALEX]
"Knowledge graph" sounds impressive. But isn't this just SQLite with extra tables?

[JAKE]
Yes. And that's the point. I ran a standalone knowledge graph as a separate service for months. Dedicated port, API, database. The agent had to make HTTP calls to access its own memory.

Like keeping your brain in a jar across the room and shouting questions at it. Cortex puts the brain inside the skull. No network calls. Memory is just there.

[SAM]
Who is this for? LangChain has memory modules. Every SDK has some kind of persistence. Why would someone pick this?

[JAKE]
Those modules store conversation history. Maybe some key-value pairs. That's not memory — that's a chat log with search.

Real memory has structure. When I fix a bug, the graph connects that commit to the project, to the error that caused it, to the correction I got for handling it wrong the first time. Next week, someone says "fix the dashboard." The agent already knows which dashboard, which repo, what broke last time, and what mistake to avoid.

[SAM]
Can it actually do that today?

[JAKE]
Today. Typed objects — commits, corrections, projects, services. Typed relationships connecting them. Full-text search. Three ingestors auto-populating from git history, workspace files, running services. The scenario I described works because the data is already there.

[ALEX]
What about semantic search? "Find communication failures" instead of "find corrections about going silent." Full-text can't do that.

[JAKE]
Can't. Not yet. Needs embeddings. Open problem at personal scale. But ninety percent of real queries are relationship queries — "what broke last week," "which corrections repeat." Those work perfectly with typed objects. Semantic search is an enhancement, not a blocker.

[SAM]
Every framework says their memory is different. What makes this credible?

[JAKE]
I use it. This isn't a demo. Ten days running, thousands of objects from real work. Real corrections, real commits, real project context.

And the architecture matters. Other frameworks treat memory as a plugin you can swap out. Cortex can't be removed without removing the agent. When memory is external, the agent decides when to check it. When memory is embedded, the agent doesn't "decide to remember." It just knows.

[ALEX]
The security model is worth mentioning. Most frameworks let the agent police itself. Malicious input makes the agent do whatever it wants because the agent controls its own permissions.

[JAKE]
We put the gate outside. A function intercepts every tool call before the agent sees the result. Pattern matching, allowlists, escalation to the human. The agent can't manipulate a gate it doesn't know exists. Designed, not built yet — six attack scenarios, nine issues found on paper before writing code.

[SAM]
Ten days old. Thousands of objects. Works on your machine. What makes this more than a side project?

[JAKE]
Compounding. Day one, five objects. Useless. Day thirty, the agent knows your project history. Day ninety, it knows things about your work that you've forgotten. Not because someone trained a model. Because the graph grew one commit, one correction, one relationship at a time.

That's the moat. A fresh instance running the same code starts at zero. An agent with three months of graph has context that can't be replicated.

[ALEX]
Phase C is the dream. Objects carry behavior, not just data. A correction has a counter. Same mistake three times, a rule fires. The rule creates a feature card. The card gets built. The agent converts its own mistakes into code that prevents them.

[SAM]
Self-improving agents. Most exciting or scariest thing in this episode.

[JAKE]
Both. And closer than it sounds. The data layer exists. The types exist. The audit log exists. Phase C is wiring triggers to objects that are already there.

[SAM]
What's actually stopping this?

[JAKE]
People. Code is open source. Repo is public. But a framework without users is a side project with a license file. The technical obstacles — multi-model support, semantic search — those are bounded engineering. Getting strangers to trust their agent to a runtime built by one designer and a handful of AI agents? That's the real problem.

And showing the work is the only honest way to solve it. Here's what we built. Here's what's broken. Here's what we learned. If that's not interesting enough to try it, better marketing won't fix the product.

Two things to build.

First: a correction decay tracker. You log every mistake. But are you making fewer? Corrections per week, grouped by pattern. One chart that answers whether the agent is learning or just accumulating apologies.

Second: a knowledge confidence score. Every fact in the graph has a timestamp. How stale is "stale"? A score that decays over time, factoring in source reliability and conflicting evidence. Real-time trust in what the agent thinks it knows.

That's "The Knowledge Graph Inside the Machine." I'm Jake. Catch you next episode.
